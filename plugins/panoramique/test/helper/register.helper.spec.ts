import { describe, it } from 'vitest';

describe('register', () => {
	describe('life cycle', () => {
		it.todo('should register in service on created hook', () => {});
		it.todo('should unregister in service on beforeDestroyed hook', () => {});
	});
	describe('dependencies', () => {
		it.todo('should mark the host plugin as dependent on panoramique', () => {});
	});
});