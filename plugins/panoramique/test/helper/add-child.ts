import { describe, it } from 'vitest';

describe('addChild', () => {
	describe('life cycle', () => {
		it.todo('should add child in service on created hook', () => {});
		it.todo('should remove child in service on beforeDestroyed hook', () => {});
	});
	describe('dependencies', () => {
		it.todo('should mark the host plugin as dependent on panoramique', () => {});
	});
});